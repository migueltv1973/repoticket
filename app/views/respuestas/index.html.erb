<p id="notice"><%= notice %></p>

<h1>Respuestas</h1>

<table>
 
  <thead>
  <tr>
    <th>Title</th>
    <th>descripcion</th>
 
    <th>Acciones</th>
    <th colspan="3"></th>
  </tr>
</thead>

  <tbody>
    <% @respuestas.each do |r| %>
    <tr>
      <td><%= r.title %></td>
      <td><%= r.descripcion %></td>
    
        <td><%= link_to 'Show', respuesta %></td>
        <td><%= link_to 'Edit', edit_respuesta_path(ticket) %></td>
        <td><%= link_to 'Destroy', respuesta, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Respuesta', new_respuesta_path , class: "btn btn-primary" %>
