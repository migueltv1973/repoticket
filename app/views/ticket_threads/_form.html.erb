<%= form_with(model: ticket_thread, local: true) do |form| %>
  <% if ticket_thread.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ticket_thread.errors.count, "error") %> prohibited this ticket_thread from being saved:</h2>

      <ul>
      <% ticket_thread.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
