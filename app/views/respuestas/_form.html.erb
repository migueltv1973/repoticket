<%= form_with(model: ticket_thread , local: true) do |form| %>
  <% if respuesta.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(respuesta.errors.count, "error") %> prohibited this respuestum from being saved:</h2>

      <ul>
      <% respuesta.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
